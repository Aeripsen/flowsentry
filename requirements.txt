# Runtime + dev + dashboard deps, for `pip install -r requirements.txt`.
# The canonical install is `pip install -e ".[dev]"` (see pyproject.toml); this file
# is a convenience mirror that also includes the dashboard and build backend.
setuptools>=61
wheel
scikit-learn>=1.4
pandas>=2.0
numpy>=1.26
joblib>=1.3
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
pytest>=8.0
ruff>=0.4
streamlit>=1.30
